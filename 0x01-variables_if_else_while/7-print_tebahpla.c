#include <stdio.h>

/**
* main - Print all the alphabet in lowercase in reverse using only putchar
*
* Return: Always 0 (Success)
*/
int main(void)
{
	char alpha
	for (alpha = 'z'; alpha >= 'a'; c--)
		putchar(alpha);
	        putchar('\n');
	return (0)
}
